# SMARTAGENT extensions catalog

> Human/orchestrator reference (CLI-flag shapes). The AGENT-facing tool brief is AGENT_TOOLS.md → "## Tools" — pi injects that file, NOT this one. Keep BOTH current when tools change.

Every tool `./pi` exposes, one row per `extensions/*.ts`. **When you add, rename, or remove an extension, update this table in the same commit** — it is the canonical description of the agent's capabilities.

| Tool | Backing crate | What it does |
|------|---------------|--------------|
| `akurai-router` | — (provider) | Registers AkurAI-Router models (claude/*, codex/*) as the pi provider |
| `semdb` | `semdb` | Semantic DB: embed, search (`--ids-only`/`--meta-chars`/`--filter key=value`), get, del, count/ids (`--prefix`), stats |
| `memory` | `memory` | 3-tier memory: remember, update, recall (`scope` tier), recent, forget, promote, stats. `project` scopes to a workspace repo's own store (`.smartagent/memory`). Session intents auto-recalled at launch |
| `codegraph` | `codegraph` | Multi-language code graph (Rust, Python, JS/TS, Go, Java, C/C++, C#, Kotlin, Scala, Ruby, PHP, Swift): index, defs/refs/callers/impls (`--limit`), path (BFS call-path), semantic search, stats. `--project` = that workspace repo's own graph (`.smartagent/codegraph.json`) — per-repo graphs never clobber |
| `codeindex` | `codeindex` | Fast code search + workspace project index: search/files (`--project` scopes to a workspaces/ repo), projects (list repos + index status), index (per-repo file inventory → `<repo>/.smartagent/codeindex.semdb`, one project or all) |
| `vault` | `vault` | Markdown brain: new, read (`--head`), append, rm, mv (link-rewrite), list, links, graph (`--note`/`--depth`), search. For durable cross-repo/project documentation prefer AkurAI-Notes MCP first; vault is fallback/mirror |
| `goal` | `goal` | Work until an independently-verified condition holds (Claude Code `/goal` parity): set/status/clear/check/run. After each turn an INDEPENDENT small model (default `claude/claude-haiku-4-5-20251001`, config `goal_eval_model`) reads the pi session transcript (`.pi/sessions`) and returns met/not-met — never the worker grading itself. `check` = the `goal-check` stop hook: unmet → `{"decision":"block","reason":…}` (reason = continuation guidance); met → clears. `goal run "<prompt>"` spawns repeated headless `./pi -p` turns until check passes or `--max-turns` is hit. State is per-session in `data/goal.semdb` (`--session`/`PI_SESSION_ID`) so gateway agents do not clobber one another. Interactive TUI caveat: `agent_end` is notification-only, not an auto-start mechanism. Command: `extensions/goal.ts` (`/goal`) |
| `skills` | `skills` | Agent Skills loader + self-authoring (procedural memory), full Agent Skills standard: list (`--role`/`--task` filter), show (`--head`, or `--path scripts/foo.sh` for Level-2 disclosure of one supporting file), search, match (prompt-scored auto-trigger, `--role`/`--task` filter), files (`--name`, list a skill's `scripts`/`references`/`assets` — relative path + size), validate — plus write verbs `create` (`--name`/`--category`/`--desc`/`--role`/`--task`, body via `--file`/stdin; rejects a name collision — role/task persist under `metadata.smartagent.role`/`.task`), `patch` (`--old`/`--new` exact-string replace, errors if not unique), `edit` (full-body rewrite), `delete` (removes the whole skill dir incl. supporting files), `write-file` (`--name`/`--path`/content via `--file`/stdin — path MUST start with `scripts/`, `references/`, or `assets/`; `scripts/` files are made executable 0755, others 0644; hard traversal guard rejects `..`/absolute/disallowed-subdir paths), `remove-file` (`--name`/`--path`, prunes the now-empty subdir but never the skill dir). `--project P` on all verbs scopes to that workspace repo's own accumulated skills (`workspaces/P/.smartagent/skills`) — reads merge global+project (project wins a name collision); writes target the project dir when given, else the positional `<root>` (global, default `./skills`) |
| `schedule` | `schedule` | Durable scheduler: add (`--notify`; `--cron` recurring OR `--at` one-shot), pause/resume, list/next/rm/tick. Supervised daemon fires jobs |
| `search` | `search` | SearXNG search: query (`--time-range`/`--site`/`--urls-only`/`--snippet-chars`, k=5), health. Wrapped UNTRUSTED |
| `notify` | `notify` | Push notifications (ntfy): send. Bearer auth, when configured, comes from policy-gated secret `ntfy_token`; no secret value appears in tool parameters |
| `telegram` | `telegram` | Telegram Bot API bridge: send (`--chat`, `--text`, 4096 chunking), poll (`getUpdates` offset in `data/telegram.semdb`), listen, commands (Bot API `setMyCommands`; reopen chat/app if Telegram caches old menu). Slash commands share `config/slash_commands.tsv` with the TUI; `/assign` opens an inline multiple-choice agent-team picker (Coordinator/Builder/QA/Ops). Token only via `secrets get telegram_bot_token`; allow-list via `telegram_allowed_chats` |
| `secrets` | `secrets` | Policy-gated secret store: set/get(as 'pi')/list/audit. Deny-by-default; policy-allow is admin-only, OFF the agent surface |
| `browser` | `browser` | Real Chrome CDP: open/click/type(`--enter`)/back/wait/scroll/attr (`--quiet`/`--max-text`/`--max-links`), probe. Wrapped UNTRUSTED |
| `sa-browser` | `sa-browser` | Visual browser: right-side 50% TUI overlay pane, bottom-filled (functional address bar + loading status + truecolor page art — braille 2×4 px/cell default, sextant/quad/half fallbacks; tablet viewport emulation default; std-only zlib/PNG/renderer in Rust); activate/deactivate/open/click/type/snapshot/status/probe, bare-host URLs, `/sab` toggle plus `/sab address`, `/sab click <selector>` and `/sab type <selector> <text>`. Successor-in-waiting to `browser` (both compose browser::cdp — port verbs, retire, rename) |
| `orchestrate` | `orchestrate` | Fan out N headless-pi subagents: run, list, out (collect output). Fork-bomb depth guard |
| `mcp` | `mcp` | MCP client (stdio+HTTP/HTTPS): tools (`--names-only`/`--filter`), call (`--head`). argv exec, no sh -c; HTTP calls have per-request timeouts. Configured server: AkurAI-Notes endpoint `https://akurai-notes.olibuijr.com/mcp` (`mcp_akurai_notes_url`, hosted at `https://akurai-notes.olibuijr.com`) — priority documentation destination for all repos/projects |
| `sandbox` | `sandbox` | Isolated exec: run (`--tail`, 16KB cap, `--stdin`, `--no-isolate`), clean. Env scrubbed + secrets tmpfs-masked, isolation ON |
| `context` | `context` | Principal identity/context loader (TELOS): compose/validate/stat |
| `evals` | `evals` | Trace/score/diff + self-heal: log, score (`--min-pass`/`--fail-only`), diff, runs, triage (`--tasks-db`/`--dry-run`/`--all` — failing runs → deduped board tasks, cursor-incremental, p1 escalation; wired to the stop hook) |
| `rag` | `rag` | Document RAG: ingest (file or `--url`), retrieve (`--doc-id`/`--snippet-chars`/`--ids-only`), get, delete-doc, stats. `--project` = per-repo corpus (`.smartagent/rag.semdb`) |
| `tasks` | `tasks` | Kanban board: board/add/todo/next/move/done/show/list/crit/block/wip/metrics. `--project` = that workspace repo's OWN board (`.smartagent/tasks.semdb`) — tasks never mix between repos. WIP limits + criteria-gated done enforced in Rust; methodology in `skills/Kanban` |
| `workflow` | `workflow` | Markdown-defined process engine (PAI skill-per-step pattern): list/show/start/step/advance(evidence-gated)/runs/abort/**drive** (engine-drives-pi: each step spawns a fresh headless `./pi` that gets ONLY that step's instruction; the driver validates the step's final `EVIDENCE:` line in Rust and advances/aborts — the model never self-advances; nested drive refused via SMARTAGENT_DRIVE). `--project` keeps run state on the repo whose tasks board the run links. Definitions in `workflows/` + `skills/*/Workflows/` |
| `voice` | `voice` | STT/TTS bridge (Pipecat concept). BUILT+TESTED but DELISTED (extensions/disabled/) — no titan speech server deployed |
| `supervise` | `supervise` | Internal process manager: status/up/down/restart of the scheduler + gateway + telegram listener (disabled by default until token/allow-list are configured) + chromium services |
| _(service, no pi tool)_ | — | `gateway` (meðvitund, Plans/MEDVITUND.md): persistent agent service — owns a long-running `./pi --mode rpc --session-id gw-<agent>` child so the agent survives client disconnects. `serve [--autonomous] [--heartbeat-secs N]` / `send` / `steer` / `attach` (interview mid-work, Ctrl-D detaches) / `status` / `stop` over unix socket `.pi/gateway.sock`. 2-min heartbeat injects time + board doing/ready + ONE-thing-at-a-time rule (`steer` when busy; queued at zero cost when idle; `--autonomous`: idle beats prompt the agent to pull and work the next single ready task). Every beat/turn/user message rows into `data/medvitund.semdb` — the agent's interviewable self-history. Runs under `supervise` (enabled) |
| _(no tool)_ | — | `session-memory.ts`: stores session intent on shutdown, recalls recent at launch |
| _(no tool)_ | tasks, skills, codeindex, workflow, hooks, memory, supervise | `commands.ts`: instant TUI slash commands via `pi.registerCommand`, using the shared `config/slash_commands.tsv` catalog also consumed by Telegram — `/board [project]`, `/tasks`, `/skills [query]` (list, or match when a query is given), `/status` (one-shot render of the supervise/codeindex/tasks/workflow statusline probes), `/index [project]` (default `--all`), `/projects`, `/runs`, `/audit` (last 10 hook firings), `/memory <query>`, plus parity aliases such as `/commands`, `/team`, `/sab`, and `/assign` (Telegram displays the actual multiple-choice agent picker). Each shells out to the Rust binary and prints straight into the chat as a `[command]` custom message (`pi.sendMessage`, display-only — NO model round-trip; the model sees it as context next turn). 60s timeouts (600s for `/index`), error text on failure |
| _(no tool)_ | `hooks` | `hooks.ts`: user-configurable lifecycle hooks (config/hooks.conf + hooks.d/) — tool_call block/rewrite, user_prompt block, session_start context injection, stop audit. Claude Code stdin-JSON/exit-2 contract; firings audited to data/hooks.semdb. Seeded: guard-destructive (sandbox), goal-check (/goal verifier), require-doing-task (edit/write kanban gate — .scratch/ and SMARTAGENT_HOOKS_RELAX=1 exempt), session-brief (live board/workflow/index context at agent start), stop-board-audit |
| _(no tool)_ | `tui-render`, gateway/tasks/workflow probes | `agentpanel.ts`: non-tool TUI command/event extension — auto-opens the right-side **AGENT TEAM** sidebar and registers `/team` to toggle it. Shows gateway fleet agents, current shared board task, running workflows, tool/usage hints, and an offline/remedy state; it is non-capturing so chat keeps keyboard focus and it reserves sidebar width for the statusline layout. |
| _(no tool)_ | all crates | `statusline.ts`: TUI statusline — per-tool ⚙/✓/✗+duration footer statuses (ANSI-colored) on tool_execution events, plus a THREE-line belowEditor widget grouped by scope: workspace `⌂` (🕸 codegraph, 🗃 codeindex repos-indexed+files, 📋 tasks, ▶ workflow), data `▦` (🧠 memory, 📚 corpus, ⏰ schedule, 📊 evals, 🤖 orchestrate), infra `⛭` (supervise services, 🧱 sandbox, 🔑 secrets auth, 🌐 chrome, 🔎 searx, 🪝 hooks). Each segment is a Rust `<crate> statusline` verb emitting `level\|icon text` (ok/warn/err decided in Rust); TS only colors (green/yellow/red), collapses healthy segments to `icon Name ✓` (detail shown only on warn/err), and column-aligns the grid. Refreshed at session start, after related tool runs, every 30s |
