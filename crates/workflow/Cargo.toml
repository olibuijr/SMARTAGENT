[package]
name = "workflow"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tasks = { path = "../tasks" }
httpc = { path = "../httpc" }
semdb = { path = "../semdb" }
