[package]
name = "codegraph"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
semdb = { path = "../semdb" }
httpc = { path = "../httpc" }
codeindex = { path = "../codeindex" }
