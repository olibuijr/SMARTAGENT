# CLAUDE.md — SMARTAGENT

> Thin pointer. Read **[AGENTS.md](./AGENTS.md)** first — architecture, crate map, reference repos, conventions, **Gotchas**. Read **[ISA.md](./ISA.md)** — system of record, ISC state.

**Status (v0.2.0+, 2026-07-03):** 27 crates done, tested, committed — semdb, httpc, memory, codegraph, codeindex, vault, skills, schedule, search, notify, secrets, browser, sa-browser, orchestrate, mcp, sandbox, context, evals, rag, voice, supervise, tasks, workflow, hooks, gateway, telegram (+desktop-agent GUI). 22 active pi tools. `sa-browser` = visual browser: right-side 50% TUI overlay pane (address bar + loading status + high-DPI sextant truecolor page art, tablet emulation, bare-host URLs; std-only zlib/PNG/renderer), `/sab` toggle. **Meðvitund gateway = 8-agent autonomous fleet** (linus/ada/grace/ken/dennis/margaret/turing/woz, famous-dev roster) + non-autonomous `jeeves` chat agent; work-chaser re-prompts on turn-end for back-to-back task pickup; `tasks add`→ready auto-queues to the fleet; self-heal loop (eval failures → deduped criteria-gated board tasks via stop hook). **Telegram bridge** (`crates/telegram`, curl transport): streaming replies (live editMessageText via `gateway ask --stream`, hermes-style), slash commands (/board /tasks /status /agents /runs /skills /memory), task-completion broadcasts; bot @olafurs_bot, chat allow-listed. Sidebar (`/team`): full-height cards with pixel-art avatars, context-usage bar, task sub-cards, RUNNING workflows. **Watchdog fix (2026-07-03):** supervise now adopts already-running service instances (argv[0]-basename match) instead of spawning duplicates — fixed the gateway restart churn. `cargo test --workspace` green (desktop-agent excluded — separate agent's WIP); `./build.sh` gate lints extensions and smoke-tests 22/22 tool registration; no file >1000 lines; zero crates.io deps. Remote: `github.com/olibuijr/SMARTAGENT` (private). 2026-07-02: 12-subagent tool review → all 10 P0 bugs fixed; ranked P1 backlog done (`Plans/TOOL_REVIEW_2026-07-02.md` holds the remaining big-ticket items); kanban `tasks` + evidence-gated `workflow` engine + `skills/Kanban`; Claude-Code-style `hooks` system. **Workspace project layer (2026-07-02):** repos under `workspaces/` are first-class projects — codeindex `projects`/`index` builds per-repo file inventories (27/27 repos indexed OK), and `--project <name>` (shared `semdb::workspace`, traversal-guarded) scopes tasks (per-repo kanban boards — tasks never mix), codegraph (per-repo graphs — global-slot clobbering fixed), memory, rag, and workflow to `<repo>/.smartagent/` (globally gitignored). TUI statusline: 3 scope-grouped column-aligned lines (⌂ workspace incl. repos-indexed segment / ▦ data / ⛭ infra); healthy infra segments collapse to `Name ✓`, stat segments keep details. **Project status SSOT: notes MCP note 39** ("SMARTAGENT — pure-Rust AI agent (dev status)") — *pending sync: notes MCP still disconnected as of 2026-07-02 evening; update it next session from CHANGELOG Unreleased.*

**CORE RULE — operate THROUGH the agent (2026-07-02, Óli's standing directive):** All operations of the system are executed BY the agent via `./pi` (headless `-p '<prompt>' < /dev/null`, or `workflow drive` for well-defined multi-step work) — the orchestrator (Claude) does NOT run the platform's operations directly. The orchestrator's job is **pilot supervision**: compose the prompt/workflow, run it through `./pi`, then MONITOR what the agent does (run output, `hooks audit`, `tasks board`, `workflow runs`, `.scratch/workflow-drive/<run>/` step logs) — and the moment it makes a mistake, STOP, fix the CAUSE in the platform (instructions, skills, hooks, workflow defs, crates, extensions), and re-run until it behaves. Every agent mistake is a platform bug to fix, not something to silently work around. Direct execution by the orchestrator is allowed ONLY for: building/fixing the platform itself (cargo, tests, `./build.sh`, git, editing crates/extensions/skills/docs), verifying the agent's claims (fusion spot-checks, curl probes), and when `./pi` itself is broken.

**Run the agent:** `./pi` from repo root (project-isolated: vendored runtime in `.pi/runtime/`, config in `.pi/agent/`, models via `extensions/akurai-router.ts`; default/test model `codex/gpt-5.4-mini --thinking low`). The launcher auto-loads `extensions/*.ts` and injects `AGENT_TOOLS.md` into the agent's initial context (keep it current with the extensions catalog). Headless: `./pi -p '<prompt>' < /dev/null` (redirect MANDATORY).

**Config:** all endpoints in `config/smartagent.conf` (embeddings, ntfy, searx, browser CDP `:9222`, voice — see the file, never hardcode an IP in docs). Resolve via `semdb::config::Config` (flag → env → file). `workspaces_dir`/`data_dir` resolve to repo root. Nothing hardcoded.

**Fusion workflow (always):** you implement → codex CLI tests (`codex exec --sandbox workspace-write -m gpt-5.4-mini -c model_reasoning_effort=low`) → codex reports PASS/FAIL → you verify the report with a direct spot-check before marking done.

**Memory policy (always):** store durable project facts in the SMARTAGENT-scoped semantic DB for the project root or workspace root (not raw `cwd`) under `workspaces/`. A repo/workspace-local `.smartagent/semdb` at the project root is the default convention. **Do not use Claude/Codex CLI integrated memory or any other global memory** for project facts.

When the user asks for workspace contents, report the folders/projects and files under `workspaces/`, not the repository root.

**Blocker hygiene (2026-07-03):** blocked tasks are dev-team work, not human homework. Every triage/beat must resolve blockers agent-side: unblock stale reasons, split/rescope, route off-lane/project-specific work to its project board, or create/pull the task that removes the blocker. Root-board blockers should trend to zero; any remaining blocker must name the next owner/action.

**Extensions catalog:** the full list of `./pi` tools/extensions and what each does lives in [CATALOG.md](./CATALOG.md) (human/CLI reference; the agent-facing brief is AGENT_TOOLS.md). Keep both current — update them in the same commit whenever you add, rename, or remove an extension.

Hard rules (repeated here because they bite):

- **Pure Rust, `std` only, zero crates.io deps** in every `crates/*` tool. pi extensions are thin TS glue only — no logic.
- **No file over 1000 lines.** Split into scoped, task-oriented modules before hitting it.
- **Borrow, don't invent.** Reference implementations are cloned in `.refrepos/` — read them, port the concept.
- **Verify by using**: drive each capability end-to-end from pi before claiming done.
- Embeddings/LLM inference is external (OpenAI-compatible HTTP); semdb stores/searches vectors itself.
- **All data in database tables.** `semdb` is the storage engine — persist collections as semdb tables (rows), not bespoke JSON/JSONL. Tables needing semantic recall get a vector column (row embedding); others store rows without one. Add vectors only where meaning-based lookup is needed.
- Worktree agents: branch from a committed base, commit in-worktree; orchestrator merges, re-verifies, pushes.
- Never touch `.refrepos/` contents (read-only references, gitignored).
- Router/model failures: you MAY edit `../AkurAI-Router` and deploy to prod via its `./deploy.sh`, then verify and continue.
- **NEVER use /tmp** (or any path outside the repo) for scratch, probes, test dbs, or config — everything lives inside the repo; use `.scratch/` (gitignored) for throwaways.
