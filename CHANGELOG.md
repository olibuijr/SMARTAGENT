# Changelog

## Unreleased
- **Fleet throughput:** (1) **work-chaser** — on busy→idle transition the gateway re-prompts IMMEDIATELY when unclaimed work exists (shared 20s PromptGate prevents beat/chaser double-fire); previously an agent idled up to a full 120s heartbeat between tasks. (2) **8 lanes** — fleet main,builder,qa,ops + dev1..dev4; board WIP doing 4→8, review 3→4 (one task per agent, pull discipline kept). (3) gateway children get `SMARTAGENT_GATEWAY_AGENT` so board pulls are owned @<agent> — real per-agent attribution. Observed post-restart: 5 agents mid-turn concurrently (was 1-2). Note: per-agent concurrency >1 is structurally off the table — a pi session is one serial conversation; lanes are the concurrency unit.
- **Sidebar:** per-agent task sub-card (boxed row inside the agent card, matched via board @owner), context-usage bar fixed (captures ctx on turn_end/agent_end — it only updated on tool events before), statusline lays out around the open sidebar (see prior entry).
- **Sidebar-aware responsive statusline:** the 3-line statusline now lays out for the REMAINING columns when the AGENT TEAM sidebar is open (shared `layout` channel in extensions/lib — agentpanel registers its width on open/close, statusline reads `availableColumns()`), verified across 120/160/200/240-col terminals with the sidebar open and closed. Sidebar itself: framed rounded-corner card per agent (accent border working, gray idle), context-usage bar, 47-col width, circled avatars.
- **Ultrathink review round:** clippy → 0 warnings outside fleet-owned crates; default output caps everywhere an agent-facing tool could flood context (tasks board/schedule/vault/mcp/evals/memory — override flags on all); secrets audit control-char JSON bug fixed; devtools_base deduped; dead code removed; AGENTS.md catalog → CATALOG.md (−1.8k tokens/session) + top-5 tool schemas trimmed (−~500 tokens/turn); deferred findings boarded (T-128 pipe-fill deadlock p2, T-129..131 dedup/caps).
- **Self-heal loop (`evals triage` + stop hook):** eval failures now automatically become board tasks the autonomous fleet pulls. Failing runs → ONE criteria-gated `eval-fix:` task each (ready, p2), with structural loop-safety from a causal-loop analysis: open-task dedupe (no board spam per sweep), runs owned by an open `T-n` skipped, **p1 escalation** instead of retry when a completed fix fails again (recurring red = design problem), an explicit no-gaming criterion (root-cause fix, no expectation weakening, no hand-logged green traces), **latest-trace-per-case scoring** (re-logging a real passing trace flips a case green), and a **sweep cursor** so append-only history never bursts tasks (first sweep initializes and skips history; `--all` overrides). Wired as `hooks.d/eval-triage.sh` on the `stop` event — verified firing live (25ms). Hardening follow-up boarded as T-120 (write-boundary hook, trace provenance, suite-strength watchdog). Advisor round added a first-writer-wins sweep lock (120s stale expiry) and a `data/eval-triage.off` kill switch. Within the hour the loop closed autonomously: triage-created T-100/T-101 were pulled and completed by the fleet.
- **tasks statusline: system overview + trend** — the board segment now leads with total open tasks and the direction of the last change (`▣ 55 open ▼ · 1/4 doing · 2 ready`), persisted in a reserved `_trend` row so the arrow survives probe frequency; per-project boards get their own trend automatically.
- **agentpanel UX overhaul (previewed + fixed in a controlled TUI):** solid 256-color background + accent left edge — chat text no longer bleeds through the sidebar; fixed the 7-field `gateway agents` TSV mapping (token counts were rendered as the ⚙ tool chain after the tokens field was added); the shared board task is shown once under the header instead of duplicated under all 4 agents; working agents get full blocks (spinner, tool ticker, last words, token count) while idle agents collapse to one dim line; new **RUNNING** section lists in-flight workflows with step + task — and works even when the gateway is down (clear ✖ offline state with remedy hint).
- **httpc json O(n²) string-parse fix:** the JSON string parser re-validated the entire remaining buffer per character — a multi-MB payload (CDP screenshot base64) took ~47s CPU. Now bulk-copies ASCII runs and validates only the current char's bounded slice: sa-browser pane render 49.9s → 2.4s wall; every JSON consumer (search, mcp, orchestrate, evals, semdb embed) benefits. Regression tests added.
- **agentpanel focus-steal fix:** the AGENT TEAM sidebar auto-opened at session_start without `nonCapturing: true` (comment claimed it, code omitted it) — keyboard focus went to the overlay and the chat editor was dead in every new TUI session. Also committed the previously-untracked `extensions/lib/statusline-common.ts` that committed extensions already import (fresh clones were broken). Verified end-to-end in a headless tmux-controlled TUI at 1920×1080-equivalent geometry (240×67): /sab visir.is → address bar, 60 sextant art rows, bottom-filled pane, typing stays in chat.
- **`./tui` — 2x2 tmux cockpit:** pi TUI (top-left) · live DA `gateway attach` (top-right) · 5s board watch (bottom-left) · meðvitund transcript tail (bottom-right). Ctrl+Alt+q/w/e/r selects panes by position, Alt+Enter toggles fullscreen zoom on the active pane. Runs on its own tmux socket (`-L smartagent`) so the root-table bindings never leak into normal tmux; re-running attaches to the live cockpit.
- **sa-browser (`crates/sa-browser` + `extensions/sa-browser.ts`):** visual browser for the TUI. On `activate` the terminal splits — chat keeps the left half and keyboard focus; a nonCapturing pi-tui overlay (width 50%, anchor top-right) fills the right half with an address bar (url + title), a loading status (⏳ loading → ● complete), and the live page rendered as high-DPI half-block truecolor art (2 color-true pixels/cell — the terminal maximum). All pixel work is std-only Rust: zlib/DEFLATE inflate (stored+fixed+dynamic Huffman, adler32-verified), PNG decoder (8-bit gray/RGB/RGBA, all 5 filters, loud rejects for what Chrome never emits), box-filter downscale with 1:2 cell-aspect fit, run-length SGR emission with per-line resets. CDP screenshot (`Page.captureScreenshot`) + `page_status` added to `browser::cdp` — the shared seam for the migration plan: port `browser`'s interaction verbs into sa-browser, retire `browser`, rename sa-browser→browser. Tool actions: activate/deactivate/open/snapshot/status/probe (snapshots UNTRUSTED-fenced, ANSI stripped from model-facing output); `/sab [url]` toggles the pane with no model round-trip. Gate now 21/21 tools. Same-night v2 from live use: **sextant pixels** (2×3 px/cell via Symbols for Legacy Computing, 2-means fg/bg clustering — 3× the DPI of half-blocks; `pixels` sextant|quad|half), **tablet viewport emulation by default** (`Emulation.setDeviceMetricsOverride` 768px dsf 2, emulated height derived from the pane grid so the art **fills the pane to the bottom row**; `device` tablet|none), and **bare-host URLs** (`/sab visir.is` → `https://visir.is`, site redirects handle www).
- **Desktop GUI (`desktop-agent/`, native eframe/egui):** Claude-Desktop-style app that drives the **real** `./pi` agent over its RPC mode (`./pi --mode rpc`) — parity by construction (same launcher, extensions, session store). Three tabs (Chat / Cowork / Code) stream real tokens + tool cards; sidebar lists/resumes real `.pi/sessions/*.jsonl`; inspector shows real token/context stats + statusline widget rows. Composer has a live **slash-command palette** (from `get_commands`), **model** + **thinking-level** pickers (from `get_available_models`), and `@`/drag-drop file attach. **Cowork** modeled on Anthropic Cowork: task-suggestion chips, plan-before-act toggle, Scheduled section, Work-in-a-Folder, interactive kanban (add/advance/done → `tasks` binary). **9 Tools panels** (workflow/memory/vault/schedule/services/hooks/evals/orchestrate/mcp) shell to the real binaries. Session actions: rename/compact/duplicate/export-HTML/fork/delete. Icons via embedded JetBrains Mono Nerd Font (Font Awesome) fallback. Follows the system light/dark theme live (XDG portal). Deps: `eframe` + in-repo `httpc` only, zero crates.io. 22 unit tests, clippy-clean, 18 focused modules (all <1000 lines), system-of-record `desktop-agent/ISA.md` (183 ISCs). Two-agent gap audit caught + fixed 4 shipping no-op RPC handlers (setStatus/setWidget/editor/error read wrong wire fields). **Known gaps:** panels not `--project`-scoped; a few minor protocol niceties (cycle_model, set_steering_mode, get_tree branch-navigator, native image→base64) unwired. Launch: `target/release/desktop-agent` (KDE entry `agent.desktop`).
- **Meðvitund gateway (`crates/gateway`, Plans/MEDVITUND.md):** persistent agent service — a supervised daemon owns a long-running `./pi --mode rpc` child (session `gw-<agent>`), so the agent survives client disconnects; clients attach/detach over unix socket `.pi/gateway.sock` (`gateway serve|send|steer|attach|status|stop`). 2-minute heartbeat composes LOCAL state (wall-clock, session age, board doing/ready, active workflow) and delivers it as `steer` when busy / zero-cost queue when idle / autonomous work-prompt with `--autonomous` (pull ONE ready task, work it end-to-end, close, repeat — one agent does one thing at a time). Every beat, turn, and user message lands in the `medvitund` semdb table — the agent's interviewable self-history. Registered as an enabled `supervise` service; model comes from `./pi` launch settings untouched.
- **httpc connect timeout (freeze fix):** `TcpStream::connect` replaced with `connect_timeout` (capped 10s, per-request `timeout_secs`-derived) across resolved addresses — a blackholed endpoint (e.g. titan VPN peer down at `100.88.0.2:8081`) now fails fast with `connect <addr>: unreachable within Ns` instead of hanging `memory recall`/embeddings calls indefinitely and freezing the TUI mid-tool-call. Root cause of the 2026-07-02 interactive freeze; Ctrl-C-during-hung-tool remains open as board T-16. Regression covered by TESTPLAN T19 (blackhole fail-fast probe).
- **QA harness (agent-authored, `Testing/`):** 50-step re-runnable test scenario covering all 20 tools + platform guards — `Testing/TESTPLAN.md` (plan v1.3: phases A–I, stable IDs T01–T50, parameterized `qa-run-<N>` run markers, regeneration/defect/teardown protocols, amendment doctrine) and per-iteration checkbox sheets `Testing/runs/RUN-<N>.md`. Authored end-to-end BY the pi agent through its own hook-gated kanban loop (orchestrator composed prompts + audited only); regeneration protocol dry-run-proven (RUN-002 generated, verified, discarded); T01 executed pass in RUN-001. Defects found by the harness live on the root board (T-16..T-18).
- **ISA reconciliation + gate hardening:** root `ISA.md` now reflects verified current state for the original ISC-1..36 checklist (75/80 overall). `build.sh` now bounds the optional project-status embedding step with `SMARTAGENT_STATUS_EMBED_TIMEOUT` (default 15s) so an unreachable titan embeddings endpoint falls back to the placeholder semdb row instead of hanging after the build/test/tool-registration gate has passed.
- **Skill coverage complete (2× Fable agents):** six new skills cover every tool and operation — `platform` (SMARTAGENT×pi expertise: architecture, features, conventions, paths, FAQ — instant platform answers), `code-nav` (codeindex+codegraph), `memory-recall` (memory/rag/vault/semdb/context — what goes where), `web-research` (search+browser+ingest, UNTRUSTED discipline), `ops` (supervise/schedule/notify/secrets/sandbox/mcp/hooks), `orchestration` (orchestrate vs workflow drive vs start + evals). All match-proven rank-1 on realistic queries; `skills validate` 14/14. Fixed: skills discovery now skips dot-dirs/target/node_modules/workspaces (`.refrepos` SKILL.md files leaked into matching); triage/retro steps route the `kanban` skill; operating-loop step 1 routes platform questions.
- **Slash commands (`extensions/commands.ts`):** instant TUI commands with zero model round-trip via `pi.registerCommand` + `sendMessage` — /board, /tasks, /skills [query], /status, /index [project], /projects, /runs, /audit, /memory <query>. Output lands in chat (and next-turn context).
- **Engine-drives-pi (`workflow drive`):** paradigm inversion of `workflow start` — the harness owns control flow. `workflow drive <name> --task T-n` runs a definition step by step; each step spawns a fresh headless `./pi` given ONLY that step's instruction + task context, and the Rust driver validates the step's final `EVIDENCE:` line (shared `valid_evidence` mandate) before advancing — retries on failure/timeout/trivial evidence, aborts the run otherwise. Step transcripts land in `.scratch/workflow-drive/<run>/`; nested drives refused (`SMARTAGENT_DRIVE` guard). New driveable `workflows/status-report.md` smoke; extension gains the `drive` action (30-min leash).
- **Enforced agent operating loop:** AGENT_TOOLS.md "How to work" replaced by a 7-step ordered loop (skills match → tasks pull → workflow task-run → investigate cheap→expensive → execute → verify → close), stated once and backed by deterministic hooks — `require-doing-task` blocks pi's edit/write while nothing is in `doing` (root board or the target workspace repo's own board; `.scratch/` + `SMARTAGENT_HOOKS_RELAX=1` exempt; block message = exact unlock commands), `session-brief` injects live board/workflow/index state at agent start, `stop-board-audit` snapshots the board into the hooks audit trail. Fixed latent `hooks.ts` crash: `before_agent_start` context injection returned the wrong shape ("content is not iterable") — never exercised until a session_start hook existed.
- **Workspace project indexing (codeindex):** every repo under `workspaces/` is a first-class project — `projects` lists repos + index status, `index <name>|--all` builds a per-repo file inventory at `<repo>/.smartagent/codeindex.semdb` (structural semdb rows, no embeddings needed) with per-repo OK/FAIL + skipped-non-repo reporting; `search`/`files --project <name>` scope to one repo using its own .gitignore. `.smartagent` added to the ALWAYS-skip list; latent `positional_dir` bug fixed (flag values mistaken for the search dir). All 27 workspace repos indexed 27 ok / 0 failed via a live `./pi` run.
- **Per-project scoping across tools (`--project <name>` / `project` param):** shared `semdb::workspace` module (root/list/resolve/data_path, traversal-guarded) resolves `<repo>/.smartagent/<store>` — per-repo kanban boards (`tasks.semdb`: tasks never mix between repos, per-repo WIP), per-repo code graphs (`codegraph.json`: fixes silent clobbering of the single global slot; 0-symbol note for non-Rust repos), per-repo memory (`memory/`: memory-policy alignment), per-repo rag corpora (`rag.semdb`: friendly no-corpus error), per-repo workflow run state (`workflow.semdb`: pairs with the repo's board). Kept host-global by design: vault, evals, schedule, secrets, session intents.
- **semdb `put_many`:** bulk insert with a single fsync (per-put `sync_data` = one fsync per row — minutes for a large code index).
- **Statusline v2:** widget refactored from 2 crowded lines to 3 scope-grouped lines — ⌂ workspace (Code, Index repos+files, Tasks, Workflow) / ▦ data (Memory, Docs, Schedule, Evals, Agents) / ⛭ infra (Services, Sandbox, Secrets, Browser, Search, Hooks); new `codeindex statusline` verb (`ok|🗃 27/27 repos, 2554 files`, warns on unindexed/stale); segments column-aligned via wcwidth-accurate padding (text-presentation glyphs like 🕸/🗃 are 1 cell in kitty); infra health segments collapse to `Name ✓` when ok while stat segments keep details; tasks statusline text humanized (`0/1 doing · 2 ready · 1 blocked`).
- `.smartagent/` added to the user's global gitignore so per-repo state dirs never dirty workspace repos' `git status`.
- Embeddings + voice endpoints wired to the akurai-vpn overlay (titan at 100.88.0.2:8081) — works off-LAN; LAN address 192.168.1.119 remains as fallback knowledge in docs.
- build.sh gate now writes an autonomous per-build status snapshot into the project semdb (.smartagent/semdb/project.semdb): embedded when the endpoint is up, placeholder row offline, never fails the build.

## 0.2.0 — 2026-07-02
- Tool review P0/P1 complete: secrets caller-token auth, hooks system, kanban tasks + workflow engine, colored statusline, batch embeddings, hardened sandbox/search/mcp/notify

### Detail (post-v0.1.0 hardening + expansion)

**New crate:** `supervise` (20 total) — internal pure-Rust process manager that
spawns/tracks/health-checks/self-heals the long-running services (scheduler
daemon, headless chromium). Replaces per-service systemd; one optional seed unit
runs `supervise watch`.

**Security (all multi-agent-review P1s fixed):**
- Secrets: real ChaCha20-Poly1305 AEAD at rest (RFC 8439, pure std, verified against official test vectors) — replaced XOR; per-secret nonce, name as AAD, tamper detection.
- Sandbox: `env_clear` + tmpfs-masking of `data/secrets`+`.pi` inside a mount namespace (a sandboxed command can't read secrets); isolation ON by default.
- `mcp` execs argv (no `sh -c`); `schedule` arbitrary `--cmd` admin-gated (agent gets safe `--notify` only); `secrets policy-allow` admin-only + off the agent surface; web/search/rag output fenced in an UNTRUSTED envelope; `orchestrate` fork-bomb depth guard.
- httpc: single-pass reads, per-header transfer-encoding match, chunk-CRLF verify, redirect POST-body drop + cross-host auth strip.

**Architecture / rule compliance:**
- JSONL journals (schedule, evals) migrated to semdb tables — no bespoke JSONL remains.
- Dedup: one `json::Value` (semdb re-exports httpc::json), `semdb::http` is a thin httpc wrapper, `flag()`/`has()` unified into `httpc::args`, embeddings resolution centralized in `Config::embeddings()`.

**Task management + process engine (PAI patterns as pure Rust, 2 new crates → 22 total, 20 active tools):**
- `tasks` crate: kanban board in a semdb table — backlog→ready→doing→review→done, WIP limits (doing=1 default) and criteria-gated `done` ENFORCED in Rust, pull-based `next`, explicit blockers with reasons, cycle-time/lead-time/throughput `metrics`, `statusline`. Extension `tasks.ts`.
- `workflow` crate: markdown-defined process engine — steps in `workflows/*.md` + `skills/*/Workflows/*.md` declare `skill:` (PAI skill-per-phase routing) and `expect:`; `advance` REQUIRES evidence (trivial 'done'/'ok' rejected — the inline-verification mandate, deterministic). Run state in semdb; `statusline` warns on runs stalled >1d. Extension `workflow.ts`.
- `skills match`: prompt-scored skill auto-trigger (word-boundary token overlap, name hits 3×) — picks the right skill for a task/step, unlike single-substring `search`.
- `skills/Kanban`: methodology skill (six practices, column semantics, blocker/priority policy, anti-patterns) + three runnable workflows: `task-run` (observe→plan→execute→verify→learn, one skill per step), `triage`, `retro`.
- Gate updated to 20/20 tool registration; statusline widget gained ▣ tasks + ▶ workflow segments.

**Hooks system (new crate, 23 total):** `hooks` — user-configurable lifecycle hooks researched from Claude Code + codex CLI (3-agent research team). Config `config/hooks.conf` (additive [hook] blocks: event/matcher/command/timeout), commands in `hooks.d/`, Claude Code I/O contract (payload JSON on stdin; exit 0 allow with optional {"decision":"block"}/{"updatedInput"}/plain-text context; exit 2 BLOCK with stderr reason; timeout fails open loudly). `extensions/hooks.ts` bridges pi events: tool_call (block+rewrite), input (user_prompt block), before_agent_start (context injection), agent_end (stop audit). Firings audited to data/hooks.semdb; 🪝 statusline segment; seeded with a guard-destructive hook on sandbox — verified blocking `rm -rf /` end-to-end through pi.

**P1 backlog burn-down, wave 2:** orchestrate `--max-parallel` (width guard) + `--retries` + run results persisted to data/orchestrate.semdb; schedule per-job `last_exit` in list + cooperative tick lock (daemon+manual double-fire closed); supervise `logs <svc> --tail`, crash-loop backoff (15s→480s cap), restarts column, tighter scheduler needle; skills `validate` + tolerant discovery (one bad SKILL.md no longer hides the rest); context stat freshness (age + STALE ≥90d); mcp stdio read timeout + stderr capture (silent hangs now diagnosable) + HTTP bearer auth via `--auth-env`; notify `--click`/`--markdown`/$NTFY_TOKEN auth; search `--pageno`; evals latency n/mean/p50/p95 + diff scores each run once; statusline segments now carry bold labels (🪝 HOOKS: …) via the extension.

**P1 backlog burn-down (from the tool review):** semdb auto-exact <10k rows (HNSW-rebuild-per-query removed), batch embeddings (`fetch_embeddings`, `embed-batch`, rag ingest now ONE POST), `del --prefix`; httpc 64KB header cap + O(n) chunked reads; memory dedup-on-write (cosine ≥0.97 consolidates), explicit ts+hits meta, relevance-based eviction, ts-ordered recent; codegraph `unused` (dead-code candidates); vault `orphans` (+dead links), `tags`/`search --tag`, rename now rewrites [[x|alias]]/![[x]]/[[x#anchor]].

**P0 fixes (12-subagent tool review, all 10 fixed):**
- secrets: caller identity is now token-authenticated — `issue-token` (admin) mints per-caller 0600 tokens; `get` requires SMARTAGENT_CALLER_TOKEN/`--token` (constant-time verify, fail-closed, audited). `./pi` launcher injects pi's token; sandbox env-scrub + secrets-mask keep it from sandboxed commands.
- mcp: JSON injection fixed (tool name escaped, --args validated as JSON).
- notify: CR/LF header injection rejected in topic/title/tags.
- codeindex: `-i` with regex now lowercases the pattern (uppercase patterns never matched).
- schedule: `--at` honors `utc_offset_minutes` config (was silently UTC); impossible dates (Feb 30) rejected instead of leaking a never-firing one-shot; new `Civil::to_unix` + `days_in_month`.
- rag: ingest deletes existing doc chunks first — re-ingest no longer leaves stale orphans.
- semdb: vector dimension enforced per-db (placeholder `[0.0]` rows exempt); mixed dims used to silently mis-score.
- search: 20s timeout (hung SearXNG no longer blocks to the 60s kill) + http(s)-only instance validation (SSRF guard).
- sandbox: ulimit caps (2GB vmem, 4096 procs, 512MB file size, CPU=timeout) in both isolation paths; isolation downgrade now warns loudly instead of degrading silently.
- browser: fixed sleeps replaced with `document.readyState` polling (`wait_ready`) in navigate/click/enter/history — faster on fast pages, no race on slow ones.

**TUI statusline (new):**
- `statusline` verb on 11 crates (supervise, sandbox, secrets, browser, search, codegraph, memory, rag, schedule, evals, orchestrate) emitting a uniform `level|icon text` protocol — severity (ok/warn/err) decided in Rust: secrets token verify, searx/chrome reachability, codegraph staleness ≥7d, memory working-cap ≥45/50, schedule soonest job ETA, evals last-run pass ratio, sandbox namespace capability.
- `extensions/statusline.ts`: per-tool footer statuses (⚙ running/✓/✗ + duration, ANSI-colored, auto-clear) via pi `tool_execution_*` events, and a two-line belowEditor widget (infra `⛭` + data `▦` rows) painting each segment green/yellow/red by level. Segments re-probe after related tool runs and every 30s. Guarded by `ctx.hasUI` (no-op headless).

**Agent capabilities & continuity:**
- Session memory: session intent captured on shutdown → episodic; recent recall injected at launch.
- Voice delisted (built but no titan STT/TTS deployed) → `extensions/disabled/`.

**Tool expansion (adversarial-council driven — features + token efficiency):**
- browser: 4→8 actions (open/click/type/back + wait/scroll/attr, `--enter`, `--quiet`, `--max-text/--max-links`).
- memory: update/recent/forget/promote + tier-scoped recall.
- search: `--time-range`/`--site`, default k=5. schedule: `--at` one-shot + pause/resume.
- rag: `--url` ingest, doc-id-scoped retrieve, get/delete-doc, `--snippet-chars`/`--ids-only`.
- vault: rm/mv (link rewrite), read `--head`. semdb: count/ids, `--ids-only`/`--meta-chars`.
- codegraph: impls + path (BFS) + `--limit`. codeindex: count/files/lines modes + `-m` cap.
- orchestrate: `out` (collect output). evals: `--min-pass`/`--fail-only`. mcp: `--names-only`/`--filter`/`--head`. sandbox: `--tail` + 16KB default.
- `./build.sh` gate now lints extensions + smoke-tests 18/18 tool registration; negative-path tests added to every CLI crate; all clippy warnings fixed.

## 0.1.0 — 2026-07-02
- Initial release: 19 pure-Rust, std-only, zero-crates.io-dep crates, each a pi extension, ported from the most popular tool in its category.
- Storage: semdb (crash-safe log store + HNSW). All data in semdb tables.
- Crates: semdb, httpc, memory, codegraph, codeindex, vault, skills, schedule, search, notify, secrets, browser (CDP), orchestrate, mcp, sandbox, context, evals, rag, voice.
- Project-isolated ./pi launcher with vendored runtime, config-driven endpoints (config/smartagent.conf), and a tools brief injected into the agent context.
