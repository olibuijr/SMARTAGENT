# Project
SMARTAGENT: pi spine + pure-Rust zero-dep tool crates.

Durable cross-repo/project documentation should be saved to AkurAI-Notes first via the hosted MCP endpoint `https://akurai-notes.olibuijr.com/mcp` (`mcp_akurai_notes_url`). Use only policy-stored MCP bearer token credentials; local markdown/vault notes are fallback or code-shipped mirrors.
